# Cerberus Example

This example shows the intended input shape for a real product launch film.

The capture paths are placeholders for a local demo. When publishing this repository, either:

- include sanitized screenshots under `examples/cerberus/captures/`, or
- point `job.json` at captures generated by your own product.

Cerberus is only an example. Motion Director should stay product-agnostic.
